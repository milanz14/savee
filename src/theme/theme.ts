import { createTheme } from "@mantine/core";

const theme = createTheme({
  primaryColor: "#ACADBC",
  secondaryColor: "#9b0ece",
  tertiaryColor: "#6665DD",
  accent: "#473BF0",
  bg: "#000500",
  positive: "#38b000",
  negative: "#ef233c",
});

export default theme;
