const Budgets = () => {
  // show the categories as little pills with the category colour.
  // menu to add a new category, assign a budget and a colour.

  // assign a budget to existing default categories

  return (
    <div>
      <h1>Budgets</h1>
    </div>
  );
};

export default Budgets;
